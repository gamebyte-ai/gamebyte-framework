/**
 * UI State Management
 *
 * Reactive state system for automatic UI updates.
 */

export {
  createState,
  computed,
  isReactive,
  resolveValue,
  type StateListener,
  type ReactiveState
} from './ReactiveState.js';
